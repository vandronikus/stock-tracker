

finnhub.api.key=




finnhub.base.url=https://finnhub.io/api/v1


#endpoints
finnhub.endpoint.realtime.quote=/quote?symbol={s}
finnhub.endpoint.historical.quote=/stock/candle?symbol={s}&resolution={r}&from={f}&to={t}

#http timeouts
http.read.timeout.milliseconds=5000
http.write.timeout.milliseconds=5000
http.socket.timeout.milliseconds=5000
http.connect.timeout.milliseconds=5000

